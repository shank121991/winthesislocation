user staypoints:

This program find staypoints and hourly weights for a given user.

1. Change Geolife file source path in variable "base_path"
2. Change destination path in variable "destination"
3. Change for loop to select users. Example:
	For user 17:
	"for i in range(17, 18):"

Run the program.